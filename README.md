# xdf-specification

This repository contains the XSD (XML Schema Definition) specification for Novanta Motion Summit drives dictionaries.

## Overview

The XSD files in this repository define the structure and validation rules for XDF files used with Novanta Motion Summit drives. These schemas ensure that XDF files are consistent, valid, and compatible with Summit drive firmware and software tools.